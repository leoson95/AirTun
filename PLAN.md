# 🗺️ سند نقشه راه و مراحل پیاده‌سازی (AirTun Implementation Plan & Roadmap)

این سند مراحل اجرایی ساخت پروژه **AirTun** را گام‌به‌گام مشخص می‌کند.

---

## 🎯 فاز ۱: راه‌اندازی سرور اندروید (Android SOCKS5 Engine) [تکمیل شد ✅]
* [x] **پاک‌سازی و بهینه‌سازی کدهای اندروید:** حذف کامل وابستگی‌های اضافه قبلی و حفظ استایل‌های Jetpack Compose.
* [x] **پیاده‌سازی سرور SOCKS5 نیتیو:** پیاده‌سازی سرور پروکسی با حداقل مصرف رم (زیر ۱۵ مگابایت) با پشتیبانی کامل از TCP و UDP.
* [x] **سیستم کشف خودکار (UDP Beacon):** برادکست مشخصات به شبکه محلی روی پورت ۴۷۸۸۰ برای شناسایی آنی توسط ویندوز.
* [x] **سیستم پین‌کد ۴ رقمی:** ایجاد کلید اعتبارسنجی امن ۴ رقمی برای جلوگیری از اتصالات غیرمجاز.
* [x] **رابط کاربری تمیز اندروید:** کلید بزرگ Start/Stop، نمایش کارت پین‌کد ۴ رقمی، وضعیت فعال بودن و شمارنده حجم مصرفی اینترنت.

---

## 🎯 فاز ۲: ساخت کلاینت ویندوز (Windows Desktop Client)
* [ ] **رابط کاربری شیک WinUI 3:** پنجره استاندارد، وسط صفحه، با قابلیت جابجایی و ریسپانسیو کامل.
* [ ] **موتور کاوش خودکار (Auto-Discovery Client):** اسکن پورت ۴۷۸۸۰ در شبکه محلی و پیدا کردن خودکار گوشی‌های فعال.
* [ ] **هسته کارت شبکه مجازی (WinTun TUN Engine):** ایجاد اینترفیس مجازی و انتقال کامل ترافیک ویندوز بدون وابستگی به پراکسی‌های ناپایدار ویندوز.
* [ ] **کلید سوئیچ دو حالته:**
  * **Full System TUN Mode:** ۱۰۰٪ ترافیک ویندوز (بازی، تلگرام، گیت، ترمینال).
  * **Fast Web Proxy Mode:** تنظیم سریع و سبک پراکسی ویندوز فقط برای وبگردی.
* [ ] **مانیتورینگ زنده:** نمایش پینگ زنده به گوشی و سرعت لحظه‌ای دانلود و آپلود.
* [ ] **سیستم خروج ایمن (Safe Teardown):** حذف آنی کارت شبکه مجازی و بازگردانی تنظیمات شبکه در صورت بستن برنامه یا قطعی ارتباط.

---

## 🎯 فاز ۳: تست، یکپارچه‌سازی و خروجی نهایی
* [ ] **تست عملکردی:** تست پایداری در بازی‌های آنلاین (پینگ و پکت‌لاست)، ابزارهای خط فرمان دولوپری (`git`, `npm`, `pip`, `docker`) و تلگرام دسکتاپ.
* [ ] **پکیجینگ ویندوز:** ساخت فایل نصاب اختصاصی (`AirTun-Setup.exe`) و نسخه پرتابل مستقل بدون پیش‌نیاز.
* [ ] **بیلد نسخه نهایی اندروید:** تولید فایل APK سبک و امضاشده.
