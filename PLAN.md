# 🗺️ سند نقشه راه و وضعیت پیاده‌سازی (AirTun Implementation Plan & Roadmap)

پروژه **AirTun** برای اشتراک‌گذاری پرسرعت و امن اینترنت گوشی با ویندوز، با موفقیت در تمامی ۳ فاز اصلی پیاده‌سازی و تست شد:

---

## 🎯 فاز ۱: راه‌اندازی سرور اندروید (Android SOCKS5 Engine) [تکمیل شد ✅]
* [x] **پاک‌سازی و بهینه‌سازی کدهای اندروید:** حذف کامل وابستگی‌های اضافه قبلی و حفظ استایل‌های Jetpack Compose.
* [x] **پیاده‌سازی سرور SOCKS5 نیتیو:** پیاده‌سازی سرور پروکسی با حداقل مصرف رم (زیر ۱۵ مگابایت) با پشتیبانی کامل از TCP و UDP (`Socks5Server.kt`, `Socks5UdpRelay.kt`).
* [x] **سیستم کشف خودکار (UDP Beacon):** برادکست مشخصات به شبکه محلی روی پورت ۴۷۸۸۰ برای شناسایی آنی توسط ویندوز (`AirTunBeacon.kt`).
* [x] **سیستم پین‌کد ۴ رقمی:** ایجاد کلید اعتبارسنجی امن ۴ رقمی RFC 1929 برای جلوگیری از اتصالات غیرمجاز (`PinCode.kt`).
* [x] **رابط کاربری شیشه‌ای اندروید:** کلید بزرگ Start/Stop، نمایش کارت پین‌کد ۴ رقمی، وضعیت فعال بودن، شمارنده حجم مصرفی اینترنت و لاگ زنده (`HomeScreen.kt`).

---

## 🎯 فاز ۲: ساخت کلاینت ویندوز (Windows Desktop Client) [تکمیل شد ✅]
* [x] **رابط کاربری مدرن WinUI 3:** طراحی شیشه‌ای Liquid Glass، پنجره استاندارد، دوزبانه (فارسی/انگلیسی) و تم تیره/روشن.
* [x] **موتور کاوش خودکار (Auto-Discovery Client):** اسکن و پروب پورت ۴۷۸۸۰ در شبکه محلی و اتصال تک‌کلیک به گوشی (`LanDiscovery.cs`).
* [x] **حالت‌های دوگانه اتصال:**
  * **Full System TUN Mode:** مسیریابی ۱۰۰٪ ترافیک ویندوز از طریق کارت شبکه مجازی (بازی‌ها، تلگرام، ابزارهای برنامه‌نویسی `git`, `npm`, `pip`, `docker`).
  * **Fast Web Proxy Mode:** تنظیم آنی و ایمن پروکسی سیستمی ویندوز (WinINET) همراه با رول‌بک و بازیابی پس از کرش (`ProxySession.cs`).
* [x] **کنترل هوشمند روتینگ (Smart Routing & Bypass):**
  * سوییچ دایرکت کردن سایت‌های ایرانی (`*.ir`، درگاه‌های بانکی، سامانه‌های ملی و اپ‌های پرکاربرد) بدون عبور از تونل.
  * موتور قوانین سفارشی (Custom Rules Engine) به سبک Geosite / V2Ray با قابلیت Direct, Proxy, Block.
* [x] **تشخیص خودکار آی‌پی و کشور خروجی (Outbound IP & GeoIP):** نمایش پرچم کشور، آی‌پی پابلیک، شهر و شرکت ارائه‌دهنده سرویس (ISP) همراه با دکمه رفرش.
* [x] **مانیتورینگ و گزارش زنده:** نمایش سرعت لحظه‌ای دانلود/آپلود، حجم مصرفی، پینگ به گوشی و مدت اتصال.
* [x] **سیستم خروج ایمن (Safe Teardown):** آزادسازی منابع، بستن تونل و بازیابی تنظیمات شبکه با بستن برنامه یا از منوی System Tray.

---

## 🎯 فاز ۳: تست، بسته‌بندی و خروجی نهایی [تکمیل شد ✅]
* [x] **تست‌های واحد جامع:**
  * ۳۶ تست واحد پاس‌شده در ویندوز (`dotnet test windows/AirTun.sln`).
  * ۲۴ تست واحد پاس‌شده در اندروید (`.\gradlew.bat testDebugUnitTest`).
* [x] **پکیجینگ ویندوز:** خروجی اجرایی نهایی `windows/publish/AirTun-win-x64/AirTun.App.exe`.
* [x] **بیلد نسخه نهایی اندروید:** تولید فایل APK در مسیر `android/app/build/outputs/apk/debug/app-debug.apk`.
* [x] **داکیومنت و همگام‌سازی گیتهاب:** مستندسازی کامل `README.md` و سینک با مخزن [https://github.com/leoson95/AirTun](https://github.com/leoson95/AirTun).
